

### 📘 My-CPU 项目编码规范

#### 1. 数据表示（铁律）
-   **信号值：** 统一使用 `int` 类型的 `0` 和 `1`。禁止在逻辑函数中直接使用 `True/False`。
-   **多位数据：** 使用 Python 原生整数表示，低位对齐。例如 8-bit 数据 `0b10100001` 即为十进制 `161`。
-   **无效/高阻态：** 若需模拟三态门总线，约定 `-1` 代表高阻态（Z），仅在总线模块中使用，基础门电路不处理此状态。

#### 2. 命名约定
-   **基础门电路：** 全小写+下划线，后缀 `_gate` 可选但推荐。如 `nand`, `xor_gate`, `mux_2to1`。
-   **时序元件：** 大驼峰类名。如 `DFlipFlop`, `SRLatch`, `Register8Bit`。
-   **信号名：** 贴近硬件习惯。时钟用 `clk`，复位用 `rst`，使能用 `en`，输入输出用 `a/b/in_/out`（避免与 Python 关键字冲突）。
-   **常量：** 全大写。如 `BIT_WIDTH = 8`, `CLOCK_EDGE_RISING = 1`。

#### 3. 函数/类接口规范
-   **组合逻辑函数：** 必须是纯函数，无副作用，不接受可变对象，返回值仅为 `0/1` 或整数。
    ```python
    def full_adder(a: int, b: int, cin: int) -> tuple[int, int]:
        """返回 (sum, cout)，顺序固定"""
    ```
-   **时序逻辑类：** 必须包含 `tick(clk)` 或 `update()` 方法作为时钟驱动入口；状态变量以 `_` 开头私有化，仅通过方法访问。
    ```python
    class Register8Bit:
        def __init__(self):
            self._value = 0
        
        def tick(self, clk: int, data_in: int, load: int) -> int:
            """仅在上升沿且load=1时更新，返回当前输出"""
    ```
-   **多输出：** 一律用元组返回，禁止用字典或列表（保证顺序稳定、解包方便）。

#### 4. 注释与文档
-   **每个元件必写真值表/状态表**：放在 docstring 里，这是硬件代码的灵魂。
    ```python
    def mux_2to1(sel: int, in0: int, in1: int) -> int:
        """
        2选1多路选择器
        | sel | out |
        |-----|-----|
        |  0  | in0 |
        |  1  | in1 |
        """
    ```
-   **关键设计决策加 `# HW:` 注释**：解释为什么这样实现，而非代码本身做了什么。例如 `# HW: 用NAND实现NOT，保持元件库原子性`。

#### 5. 测试规范
-   **穷举真值表**：所有组合逻辑必须遍历所有输入组合验证，不允许只测几个样例。
-   **时序测试带时钟序列**：时序元件测试必须模拟多个时钟周期，覆盖上升沿、下降沿、保持状态等场景。
-   **测试文件命名：** `test_<module>.py`，与源码一一对应。

#### 6. 文件结构
```text
my_cpu/
├── gates.py          # 基础门电路（纯函数）
├── arithmetic.py     # 加法器、ALU
├── sequential.py     # 锁存器、触发器、寄存器
├── memory.py         # RAM、ROM
├── cpu.py            # 顶层CPU
├── tests/            # 测试目录
└── README.md         # 本规范 + 进度记录
```



---

gates.py 元件清单（共 12 个函数）

| #   | 函数名                | 签名                                      | 用途                              |
| --- | ------------------ | --------------------------------------- | ------------------------------- |
| 1   | `nand_gate`        | `(a, b) → int`                          | 与非门，**唯一原子门**，所有其他门的构建基石        |
| 2   | `not_gate`         | `(a) → int`                             | 非门，NAND 两输入短接                   |
| 3   | `and_gate`         | `(a, b) → int`                          | 与门，NAND + NOT                   |
| 4   | `or_gate`          | `(a, b) → int`                          | 或门，三个 NAND（德·摩根）                |
| 5   | `xor_gate`         | `(a, b) → int`                          | 异或门，四个 NAND，加法器核心               |
| 6   | `majority_4`       | `(a, b, c, d) → int`                    | 4输入多数表决门，≥2个为1则输出1              |
| 7   | `mux_2to1`         | `(sel, in0, in1) → int`                 | 2选1多路选择器，sel=0选in0，sel=1选in1    |
| 8   | `_bit`             | `(val, n) → int`                        | 辅助函数，提取整数第n位（仿真用，非被仿真电路）        |
| 9   | `mux_4to1`         | `(sel, in0, in1, in2, in3) → int`       | 4选1多路选择器，sel为2-bit整数（两根导线）      |
| 10  | `split_bus_8bit`   | `(data) → (b0,b1,...,b7)`               | 8-bit 分线器，总线拆成8根独立线，低位在前        |
| 11  | `collect_bus_8bit` | `(b0,b1,...,b7) → int`                  | 8-bit 收线器，8根线拼回总线，b0=LSB        |
| 12  | `mux_8bit`         | `(sel: int, in0: int, in1: int) -> int` | 8-bit 宽 2选1多路选择器（逐位调用 mux_2to1） |

---

arithmetic.py 元件清单（当前 3 个函数）

| # | 函数名 | 签名 | 用途 |
|---|--------|------|------|
| 1 | `half_adder` | `(a, b) → (sum, cout)` | 半加器，XOR + AND |
| 2 | `full_adder` | `(a, b, cin) → (sum, cout)` | 全加器，两个半加器 + OR |
| 3 | `ripple_carry_adder_8bit` | `(a, b, cin=0) → (sum, cout)` | 8-bit 行波进位加法器，8个FA串联 |

sequential.py 元件清单（当前 4 个类）完成

| #   | 类名               | 核心方法                                                                        | 用途                                                                                                | 状态    |
| :-- | :--------------- | :-------------------------------------------------------------------------- | :------------------------------------------------------------------------------------------------ | :---- |
| 1   | `SRLatch`        | `update(s, r) → (q, q_bar)`                                                 | SR锁存器（NAND实现，低有效，迭代收敛）                                                                            | ✅     |
| 2   | `DFlipFlop`      | `tick(clk, d) → q`                                                          | D触发器（主从结构，上升沿触发）                                                                                  | ✅ 新完成 |
| 3   | `Register8Bit`   | `tick(clk, data_in, load) → int`                                            | 8-bit 寄存器（含并行加载）                                                                                  | 已完成   |
| 4   | `ProgramCounter` | ` tick(self, clk: int, rst: int, load: int, inc: int, data_in: int) -> int` | 8-bit 程序计数器（PC）  <br>  <br>功能：存储下一条指令的地址，支持复位 / 跳转 / 自增 / 保持。控制优先级（高 → 低）：rst > load > inc > hold | 已完成   |







#### 7. 时序约定（铁律，与第 1 节同级）

##### 7.1 两拍时钟协议

所有时序元件遵循"主从两拍"模型：

|         调用         | 时钟  | 语义                      | 类比     |
| :----------------: | :-: | :---------------------- | :----- |
| `tick(clk=0, ...)` | 低电平 | **摆数据**：组合逻辑求值，主锁存器透明捕获 | 📷 拍照  |
| `tick(clk=1, ...)` | 高电平 | **出结果**：从锁存器透明，输出更新     | 🖨️ 冲印 |

> 一个完整时钟周期 = 一次 `tick(0)` + 一次 `tick(1)`，缺一不可。

##### 7.2 数据稳定规则

- 写入数据（`data_in`、`load` 等控制信号）
  **必须在 `tick(clk=0)` 那一拍就已传入并稳定**。
- `tick(clk=1)` 那一拍传入的数据**不会被采样**
  （主锁存器已关闭），仅用于保持语义一致性。
- 违反此规则 = 真实硬件中的 **setup time violation**。

##### 7.3 原子 tick 规则

- 每个元件、每个时钟相位，`tick()` **只能调用一次**。
- 禁止在同一相位内多次调用同一元件的 `tick()`。
- 禁止在组合逻辑函数中调用任何 `tick()`。
##### 7.4 同一 tick 内的求值顺序

在一次 `tick(clk=1)` 调用内部，执行顺序固定为：
组合逻辑（mux、加法器等）→ 时序元件（FF、锁存器）
即：先算"喂给 FF 的是什么"，再让 FF 决定"锁不锁"。
这模拟了真实硬件中"组合逻辑传播延迟 << 时钟周期"的假设。

##### 7.5 控制信号扇出
clk、load、rst 等控制信号是全局广播（fan-out），
同一拍内所有元件收到相同值。
数据总线是逐位独立（bit-slice），各位之间无耦合。

##### 7.6 时序测试规范
测试时序元件时，必须成对调用：
reg.tick(clk=0, data_in=D, load=1)   # 摆数据
result = reg.tick(clk=1, data_in=D, load=1)  # 触发
禁止只调一次 tick(clk=1) 就断言输出。
每个测试场景至少覆盖 2 个完整周期
（加载→保持、加载→覆盖、保持→保持）。
## 关键约定备忘

- 所有函数返回 `0` 或 `1`（单bit）/ 整数（多bit）
- 多输出用**元组**，顺序固定
- `_bit` 是仿真辅助，不是被仿真的电路
- `split_bus_8bit` 返回顺序：`(b0, b1, ..., b7)`，**低位在前**
- `collect_bus_8bit` 参数顺序：`b0, b1, ..., b7`，**低位在前**
- `mux_4to1` 的 `sel` 是 2-bit 整数，物理上是**两根导线**